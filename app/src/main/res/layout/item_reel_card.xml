<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="10dp"
    android:layout_marginTop="5dp"
    android:layout_marginEnd="10dp"
    android:layout_marginBottom="5dp"
    app:cardBackgroundColor="@color/white"
    app:cardCornerRadius="16dp"
    app:cardElevation="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="9dp"
        android:paddingBottom="8dp">

        <!-- Author row. Tighter than a profile header: the reel is the point. -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="12dp"
            android:paddingEnd="12dp">

            <com.github.siyamed.shapeimageview.CircularImageView
                android:id="@+id/image_view_card_avatar"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:src="@drawable/profile"
                android:contentDescription="@null" />

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="9dp"
                android:layout_weight="1"
                android:orientation="vertical">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <TextView
                        android:id="@+id/text_view_card_author"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:ellipsize="end"
                        android:maxLines="1"
                        android:textColor="@color/primary_text"
                        android:textSize="14sp"
                        android:textStyle="bold" />

                    <ImageView
                        android:id="@+id/image_view_card_verified"
                        android:layout_width="13dp"
                        android:layout_height="13dp"
                        android:layout_marginStart="4dp"
                        android:src="@drawable/ic_verified_reel"
                        android:visibility="gone"
                        android:contentDescription="@null" />
                </LinearLayout>

                <TextView
                    android:id="@+id/text_view_card_time"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@color/primary_text_light"
                    android:textSize="11sp" />
            </LinearLayout>

            <!-- Follow is a word, not a slab: it should not out-shout the reel. -->
            <TextView
                android:id="@+id/text_view_card_follow"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingStart="10dp"
                android:paddingTop="6dp"
                android:paddingEnd="4dp"
                android:paddingBottom="6dp"
                android:text="@string/reel_follow"
                android:textColor="@color/green"
                android:textSize="13sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- Media, always 4:5. Two cards then fit a screen, and the next reel is always
             part way into view - which is what keeps a feed moving. Portrait clips are
             cropped to the frame rather than sitting in black bars. -->
        <FrameLayout
            android:id="@+id/frame_layout_card_media"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="9dp"
            android:background="#111317">

            <ImageView
                android:id="@+id/image_view_card_media"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:contentDescription="@null" />

            <androidx.media3.ui.PlayerView
                android:id="@+id/player_view_card"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="gone"
                app:resize_mode="zoom"
                app:use_controller="false" />

            <ImageView
                android:id="@+id/image_view_card_play"
                android:layout_width="54dp"
                android:layout_height="54dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_play_badge"
                android:contentDescription="@null" />

            <!-- Sound is off while a card plays in the feed; the player is where it is on. -->
            <ImageView
                android:id="@+id/image_view_card_muted"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_gravity="bottom|end"
                android:layout_margin="10dp"
                android:background="@drawable/bg_reel_top_button"
                android:padding="6dp"
                android:src="@drawable/ic_reel_volume_off"
                android:visibility="gone"
                android:contentDescription="@null" />
        </FrameLayout>

        <!-- Actions. Counts sit beside their icon, and the overflow is pushed to the far
             right so a stray tap never opens the report sheet. -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="12dp"
            android:paddingTop="9dp"
            android:paddingEnd="8dp">

            <ImageView
                android:id="@+id/image_view_card_like"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_reel_heart_outline"
                android:contentDescription="@null" />

            <TextView
                android:id="@+id/text_view_card_likes"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:layout_marginEnd="18dp"
                android:textColor="@color/primary_text"
                android:textSize="13sp"
                android:textStyle="bold" />

            <ImageView
                android:id="@+id/image_view_card_share"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_reel_send"
                android:contentDescription="@null" />

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginStart="18dp"
                android:src="@drawable/ic_reel_views"
                android:contentDescription="@null" />

            <TextView
                android:id="@+id/text_view_card_views"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:textColor="@color/primary_text_light"
                android:textSize="13sp" />

            <View
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="1" />

            <ImageView
                android:id="@+id/image_view_card_more"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:padding="5dp"
                android:src="@drawable/ic_more"
                android:contentDescription="@null" />
        </LinearLayout>

        <TextView
            android:id="@+id/text_view_card_caption"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="7dp"
            android:layout_marginEnd="12dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:textColor="@color/primary_text"
            android:textSize="14sp" />
    </LinearLayout>
</androidx.cardview.widget.CardView>
